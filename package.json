{
  "scripts": {
    "build": "ubw compile",
    "test-serve": "npm run test-serve:prepare && npm run test-serve:run",
    "test-serve:prepare": "rm -rf ./test-serving && mkdir test-serving && cp -r docs test-serving/blog",
    "test-serve:run": "serve test-serving"
  },
  "private": true,
  "dependencies": {
    "unlimited-blog-works": "^0.0.7"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "serve": "^14.2.1"
  }
}
